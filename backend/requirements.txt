fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pdfplumber==0.11.1
python-docx==1.1.2
openpyxl==3.1.5
google-api-python-client==2.137.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
rank-bm25==0.2.2
rapidfuzz==3.9.7
fastembed==0.3.6
# Optional: OCR for scanned/photographed resumes (pure pip, no system binaries).
# onnxruntime already ships with fastembed, so this adds only the small models.
pypdfium2==4.30.0
rapidocr-onnxruntime==1.3.24
# Optional: LLM deep-dive over the shortlist (only used when ANTHROPIC_API_KEY is set)
anthropic==0.69.0
